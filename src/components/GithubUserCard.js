import React from 'react';


const GithubUserCard = props => {
    return (
       <>
        {/* {props.users.map(user => (
            console.log(props);
            <div className="user">
                <img alt="Users Avatar Image" src={user.avatar_url} />
            </div>
        ))} */}
       </>
    )
};

export default GithubUserCard;
